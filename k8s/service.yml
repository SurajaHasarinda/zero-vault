apiVersion: v1
kind: Service
metadata:
  name: zero-vault-service
  namespace: zero-vault
  labels:
    app: zero-vault
spec:
  type: NodePort
  selector:
    app: zero-vault
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30000 # Accessible on this port across all nodes
